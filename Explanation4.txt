PROBLEM 4: ACTIVE DIRECTORY

The is_user_in_group method requires O(n^2) time complexity to check if any given user is a member of the entire user set within all 
groups and subgroups.

The space complexity should require O(1) with the constant based on however many groups/sub-groups exist.
